import { ValidationsPuntosDirective } from './validations-puntos.directive';

describe('ValidationsPuntosDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationsPuntosDirective();
    expect(directive).toBeTruthy();
  });
});
